

def add_nugget(nugget, anatomize=True, merge_actions=True, apply_sematics=True):
    nugget


def feed_nuggets(nuggets, anatomize=True, merge_actions=True, apply_sematics=True):
    for nugget in nuggets:
        add_nugget(nugget, anatomize, merge_actions, apply_sematics)
